import { generateFortune30Partners } from '@/lib/services/data/fortune30Partners';

export const sampleFortune30 = generateFortune30Partners();
export const sampleInternalPartners = await generateInternalPartners();